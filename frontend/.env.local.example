# URL du backend (accessible depuis le serveur Next.js)
BACKEND_URL=http://localhost:3001

# URL du backend accessible depuis le navigateur (pour les appels directs)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
